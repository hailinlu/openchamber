# Agent memory
.graymatter/

# Logs
logs
*.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
release

# changelog-card tool: cached webfonts + generated cards
scripts/changelog-card/.fonts/
changelog-*.png
*.local
*.tgz
*.vsix
/npm
/tsc
/openchamber@*
local-dev*
.tmp/
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.opencode/plans/*
.hive
docs/personal/*
SECURITY_HARDENING_IMPLEMENTATION_PLAN.md

# Build outputs
build/
.gradle/
.*.bun-build

# Built webview assets (generated during build)
src/main/resources/webview/

# IDE
*.iml
*.ipr
*.iws

# Local env
.env
.env.local

# IntelliJ plugin (separate project)
packages/intellij/

# OS
Thumbs.db

# Local runtime state (docker/dev)
data/
workspaces/
*.pid
.worktrees/
test-results/

# Agent tooling artifacts
.zcode/
.codegraph/

# Rust build artifacts
rust/target/
rust/**/target/

# Rust 本机 Cargo 配置 (含 OpenSSL 路径等机器特定设置, 不可提交)
# 详见 rust/.cargo/config.toml 内说明
rust/.cargo/config.toml

# Tauri mobile icon build artifacts (desktop shell only; iOS/Android not built)
rust/oc-tauri/src-tauri/icons/android/
rust/oc-tauri/src-tauri/icons/ios/
rust/oc-tauri/src-tauri/icons/64x64.png

# oc-server 运行时生成的用户配置 (包含 relay 私钥等敏感材料,
# 与 Electron 时代的 ~/.config/openchamber 同源)
rust/oc-server/.config/
rust/**/.config/openchamber/

# Bun 是任务运行器而非包管理器 (见 AGENTS.md), 误生成的 bun.lock 与 pnpm 冲突
bun.lock
bun.lockb

# 历史阶段的 superpowers 实施计划文件 (Phase 3c Group 2 已被阶段 3f 替代完成)
docs/superpowers/plans/2026-07-15-oc-server-3c-group-2-small-model.md

# Local Git worktrees
.worktrees/

# 临时调试探针 (一次性编译产物, 仅用于本机排查; 代码中 probe_ 前缀是函数名, 非这些二进制)
rust/probe_atomic.exe
rust/probe_atomic.pdb
rust/probe_windows_paths.pdb

# Agent 工具的 brainstorm 缓存 (与 .zcode/.codegraph 同类, 机器本地)
.superpowers/

# Tauri oc-server 运行时生成的用户配置/会话状态 (含实时 session id 等本地数据,
# 与已忽略的 rust/**/.config/openchamber/ 同源, 仅目录名随产品名变化)
rust/oc-tauri/src-tauri/.config/

# Tauri 桌面构建产物的预构建 UI 资源 (构建产物, 见 scripts/tauri-build.mjs 暂存逻辑)
rust/oc-tauri/ui-dist/

# 临时 smoke-test 脚本与实施计划草稿 (本机排查用, 不入库)
docs/superpowers/plans/2026-07-17-tauri-windows-shell-parity.md
docs/superpowers/plans/smoke-detect-miss.mjs
docs/superpowers/plans/smoke-detect-only.mjs
